{
  "name": "server",
  "version": "1.0.0",
  "description": "Basic multipage website using a basic node server to direct from page to page, also sends a bsic error if any unrecognized path is taken.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
